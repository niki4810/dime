import React from 'react';
import { storiesOf } from '@storybook/react';
import Detail from '../../../src/components/modules/Detail';

storiesOf('Modules/Detail', module)
  .add('default', () => (
    <Detail />
  ));